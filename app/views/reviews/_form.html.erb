<%= form_for review do |f| %>

<%= f.label :rating %>
<%= f.select :rating, (1..5)%>

<%= f.label :musician %>
<%= f.collection_select :musician, @musicians, :id, :name%>

<%= f.label :content %>
<%= f.text_area :content %>

<%= f.submit %>
<% end %>
